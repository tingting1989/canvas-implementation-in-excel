/**
 * 排序顺序枚举
 * @description 定义数据排序的方式，用于对数据进行升序或降序排列
 * @constant {Object}
 * @property {string} ASC - 升序排列（Ascending），按从小到大的顺序排序（数字、字母、日期等）
 * @property {string} DESC - 降序排列（Descending），按从大到小的顺序排序（数字、字母、日期等）
 */
export const SORT_ORDER = Object.freeze({
    ASC: "asc",
    DESC: "desc",
});
